apiVersion: apps/v1
kind: Deployment
metadata:
  name: memorang-worker-deploy-prod
  labels:
    app: memorang-worker-prod
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: memorang-worker-prod
  template:
    metadata:
      labels:
        app: memorang-worker-prod
    spec:
      nodeSelector:
        node-type: regular
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: memorang-worker-prod
        image: tickertom.azurecr.io/memorang-backend:latest
        imagePullPolicy: Always
        command:
        - doppler
        - run
        - --preserve-env=TEMPORAL_ADDRESS,TEMPORAL_NAMESPACE,TEMPORAL_TASK_QUEUE
        - --
        - node
        - dist/temporal/worker
        env:
        - name: DOPPLER_TOKEN
          valueFrom:
            secretKeyRef:
              name: doppler-tokens
              key: WORKER_DOPPLER_TOKEN
        - name: NODE_ENV
          value: production
        - name: HOME
          value: /tmp
        - name: TEMPORAL_ADDRESS
          value: temporal:7233
        - name: TEMPORAL_NAMESPACE
          value: default
        - name: TEMPORAL_TASK_QUEUE
          value: document-ingestion
