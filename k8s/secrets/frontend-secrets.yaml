# ============================================================
# Frontend secrets template (mostly not needed — VITE_ vars
# are baked in at build time via Docker build-args).
# This file is a placeholder for future runtime secrets.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
  namespace: memorang
type: Opaque
stringData: {}
